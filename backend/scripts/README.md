# Scripts

Rules for future scripts:
- Typed `main() -> int` entrypoint
- No import-time execution
- Guard entry with `if __name__ == "__main__":`
- Scripts must be idempotent
- Log processed counts and operation metrics
